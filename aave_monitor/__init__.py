"""Aave V3 multi-chain reserve, utilization, supply, interest-rate,
and threshold-triggered activity monitor -> Telegram alerts.

See aave_monitor.app for the entry point (`main()`), also exposed via
the top-level `main.py` script.
"""
